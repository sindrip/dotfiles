[settings]
idiomatic_version_file_enable_tools = ["node"]
minimum_release_age = "0s"

[tools]
# Global Node fallback; project version files can override it.
node = "26.10.0"
"npm:corepack" = "0.36.0"
